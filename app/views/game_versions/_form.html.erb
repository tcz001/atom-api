<%= form_for(@game_version) do |f| %>
  <% if @game_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_version.errors.count, "error") %> prohibited this game_version from being saved:</h2>

      <ul>
      <% @game_version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="field">
    <%= f.label :language %><br>
    <%= f.text_field :language %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
